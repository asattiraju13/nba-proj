{
  "name": "backend",
  "version": "0.01",
  "description": "express mongo backend for nba viz project",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "cd frontend && yarn build",
    "install-client": "cd frontend && yarn",
    "heroku-postbuild": "yarn run install-client && yarn run build",
    "dev": "nodemon index.js"
  },
  "author": "Abhinav Sattiraju",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "mongoose": "^6.3.4",
    "procfile": "^0.1.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.16"
  },
  "type": "module"
}
